// tslint:disable-next-line:no-empty-interface
export interface ISoundProps {
    sound: any,
    delay: number,
    autoplay: boolean,
    play: boolean
}
// tslint:disable-next-line:no-empty-interface
export interface ISoundState {
    percent: number,
    current: string,
    total: string
}
